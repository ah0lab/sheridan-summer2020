/*
 * using specifies a namespace that the compiler should look at to 
 * find any classes that are referenced in your code but aren't defined 
 * in the current scope.
 */
using static System;

namespace sheridan.W2TextbookExamples
{
  class init
  {
    static void Main() {
      WriteLine ("Textbook Example 1: ");
    }
  }
}
